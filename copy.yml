---   
- hosts: webservers
  become: True
  tasks:
    - name: Install packages
      apt:
         name: apache2
         state: present

    - name: Start Apache service
      service:
         name: apache2
         state: started
      
    - name: Add firewall rules
	  ufw:
	     rule:allow
		 port:80
		 port:tcp
		 
    - name: Deploy static website
		 copy:
		 src: index.html
		 des: var/www/html/
...		
		
	      

   
